apiVersion: v1
kind: Service
metadata:
  name: {{ include "asir-app.fullname" . }}
  labels:
    {{- include "asir-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      nodePort: {{ .Values.service.nodePort }}
      name: http
  selector:
    {{- include "asir-app.selectorLabels" . | nindent 4 }}
