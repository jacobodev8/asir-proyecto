apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asir-app.fullname" . }}-test-connection"
  labels:
    {{- include "asir-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "asir-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
